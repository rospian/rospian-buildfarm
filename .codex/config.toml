[permissions]
# Allow read-only access to the workspace
read = true

# Require confirmation before edits
write = "prompt"

# Disallow destructive actions by default
delete = false

# Disallow shell execution unless explicitly allowed
shell = "prompt"

[permissions.paths]
write = [
  "ros2/patches/**",
  "ros2/sbuild/**",
  "ros2/patches.sh",
  "build.sh",
  "/tmp/**",
]

read = [
  "**"
]

[shell]
# Restrict shell access to known-safe commands
allowed_commands = [
  "awk",
  "colcon",
  "ros2",
  "bloom",
  "dpkg-buildpackage",
  "dpkg-deb",
  "sbuild",
  "apt",
  "apt-get",
  "cmake",
  "make",
  "git",
  "python3",
  "scripts/test_bloom_generate.sh"
]
